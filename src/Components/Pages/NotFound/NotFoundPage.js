import Layout from "../../Helpers/App/Layout/Layout";

export default function NotFoundPage() {
    console.log('in');
    return (
        <Layout>
            <div>
                no data
            </div>
        </Layout>
    )
}